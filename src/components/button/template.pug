div(class=`${isActive ? 'active' : 'disabled'}`)
  button(type=`${type === 'submit' ? 'submit' : 'button'}` data-link=`${data && type === 'link' ? data : ''}` class=`button radiused text-center full-width ${type ? `button--${type}` : ''}`) #{value}
